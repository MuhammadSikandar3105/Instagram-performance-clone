import React from "react";
import { MainContent, Messages } from "./index";

const Message = () => {
  return (
    <MainContent>
      <Messages />
    </MainContent>
  );
};

export default Message;
